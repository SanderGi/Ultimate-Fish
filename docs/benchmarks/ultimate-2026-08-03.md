# Ultimate Fish conformance and strength checkpoint — 2026-08-03

Host: Apple Silicon macOS. Candidate build: `-O3 -DNDEBUG`, C++17. All matches
used deterministic alternating colors and a fixed 5,000-node budget per move.

## Correctness and search benchmark

| Fixture | Perft / search result |
| --- | --- |
| Classic control | perft(3) = 8,900; depth 6 = 47,288 nodes / 192 ms |
| Ultimate interactions | perft(1/2/3) = 44 / 1,276 / 34,936; depth 5 = 10,979 nodes / 35 ms |

The rules suite passes with `-Wall -Wextra -Wpedantic -Werror` and again under
AddressSanitizer plus UndefinedBehaviorSanitizer. The production UI build,
rendered HTML test, linter, and local bridge integration tests also pass.

## Alternating-color matches

- Candidate versus committed `c4b46458`: **6.0/10**. The two decisive pairs
  came from the volatile special-interaction fixture; the other fixtures drew
  within 120 plies.
- The same candidate versus `c4b46458` at 20,000 nodes and 160 plies scored
  **5.0/10** (one win, one loss, eight draws), confirming no regression but
  showing that the shallow edge was not statistically meaningful.
- Forcing-action quiescence/LMR candidate versus the immediately preceding
  conformant binary: **10.0/20**. Results were color-symmetric, so this is not
  recorded as an Elo gain. The change remains because it prevents reductions
  and horizon cutoffs on source-backed Fisherman and Mage/Giant tactical
  relocations.

The AI draft completed all twelve native windows, reached 100 points for both
sides, and stayed within both 24-cell deployment zones. Future evaluation
changes should use a larger, randomized opening book and SPRT-style stopping;
the deterministic harness remains the regression gate for legality and gross
strength loss.

## Live Android checkpoint — 2026-08-04

- Six autonomous wins are recorded against newly generated Very Hard CPU
  armies: five at depth 6 and a post-unlock depth-7 checkmate.
- A fully synchronized depth-7 Unranked game ended in victory by opponent
  forfeit after the evaluation rose from +0.80 to approximately +6.10.
- The final depth-7 CPU run exercised a native Mage swap, royal ambiguity, and
  hidden Ghost movement before checkmate; a separate live run confirmed quiet
  two-step Prince continuations.
- The current identical-binary 20,000-node, 160-ply control remains exactly
  **5.0/10** across the five alternating-color fixture pairs.
- The deterministic benchmark remains classic perft(3) = 8,900 and Ultimate
  perft(1/2/3) = 44 / 1,276 / 34,936. On this host the final run searched the
  classic depth-6 control in 47,288 nodes / 186 ms and the Ultimate depth-5
  control in 10,979 nodes / 34 ms.

## Ranked draft roster iteration — 2026-08-04

The former automated roster used Ghost, Dragon, Jester, Bomb, Berserker,
Parasite, and Penguin. Equal-engine, mirrored-deployment matches tested legal
final-window duplicate stacks at 10,000 nodes per move. Four Penguins scored
2.0/2 against the mixed roster and 2.0/2 against both the four-Bomb and
four-Parasite variants. Repeating the Penguin/mixed pair at 30,000 nodes and
220 plies again scored 2.0/2. The final-pick heuristic now selects four Penguins
when they remain unbanned, while retaining the source-backed native costs,
window limits, and earlier tactical choices. A reference test fixes the policy
for both draft colors.

## Corrected-rule live validation — 2026-08-04

Live Unranked play exposed one material rules error that the static fixtures had
not distinguished: a Checker with an available first jump suppresses that
Checker's own quiet moves, but it does **not** force the entire side to move the
Checker. Only an already-started multi-jump is globally forced. The native app
accepted `bishop b9-c8` while `checker d9-b7` was available; Ultimate Fish now
accepts both, while retaining the forced continuation after the first jump. A
dedicated regression position fixes this behavior.

After that correction, the saved 100-point Giant roster completed three fresh
Very Hard CPU games at depth 7 with **3 wins / 0 losses**. The set included a
64-belief hidden-Ghost opening and two forced-checkmate finishes. Three fully
automated Unranked wins are also confirmed with the optimized roster; failed
public-position probes and opponents who did not accept an offer are excluded
from the result count.

The corrected-rule deployment tournament used three generations and four finalists,
alternating colors, and a 10,000-node finalist pass. The conventional
five-Queen roster and the live Giant roster tied at **0.8000** over ten games.
The Giant roster remains the live default because it has the stronger external
validation above; the tie does not justify rebuilding a proven native army.

## First Ranked pipeline trial — 2026-08-04

The first live Ranked draft reached all 24 pots but lost 10 rating when the
opening local Ban bubble was selected yet not recognized by OCR. No engine move
was played. The regression screenshot showed the native Ban action still
pending, so the controller now uses a bounded saturated-red component detector
and synchronous taps; the captured control is found at `(843,797)` in about
half a second. A synthetic character-body false-positive test and the captured
screen check both pass. This timeout is an automation failure and is excluded
from engine strength results.

## Ranked draft protocol correction — 2026-08-05

A second live trial ended on the draft clock before engine play and cost 8
rating. The app emitted `NetworkManager:OnBanCharacter(Type)`, while the
controller recognized only the earlier callback spelling and therefore never
submitted a local ban. The parser now accepts the shipping callback and every
local Ban/Lock is retried only until that exact native acknowledgement arrives.
A deterministic twelve-phase controller test uses the captured callback form
and fails if any phase advances without its corresponding app event.

That trial also proved that a pick can exceed 15: the opponent committed 27
opening points. A following live trial, below, supplied the remaining range
evidence. Committed groups lock in place and become public after each pick
window. Royal squares in the first group form the complete King hypothesis set;
any royal first revealed in a later locked group is therefore a known Jester.
This timeout remains classified as controller/protocol failure rather than an
Ultimate Fish strength result.

## Ranked public-group recovery correction — 2026-08-05

The next run proved the Ban repair in live play: after the opponent banned
Ghost, Ultimate Fish submitted Ninja immediately and received the native ban
callback. It then lost 7 rating before engine play because character pots
visually overlap the shallow opponent deployment. Outline/tap reconstruction
mistook pot art for occupied cells and spent the local pick clock trying to
confirm those false cells.

The retained public log removes that ambiguity. Immediately after the real
`OnSpawnPieceGroup`, the app logged cumulative material 17 then 27 and redrew
`queen 6:1` plus three royal records. In the Onyx view these sanitize to Queen
at b9 and royal silhouettes at a10/h10: fixed King 0 + Queen 17 + Jester 10.
The parser now journals those public `Square.Spawn` records, masks Ghost
coordinates before storage, and uses material only for Ghost count. No draft
board tapping is needed, including for the final pre-move Ranked position.

The same live screen displayed “more than 15 and less than 40 points.” Combined
with `SetTotalMaxPoints`, `GetDraftPhase`, and `LockinPicks`, the exact pick
ranges are: add at least 15 under cumulative 40 for both opening groups; add at
least 15/40 under cumulative 80/90 for the Ivory/Onyx middle groups; and add
zero or more under 100 for each final group. These two clock losses are
controller/protocol failures, not engine strength results.

A following run exposed a separate phase-zero side-detection error. The
controller called the player Onyx and waited, but the native response to its
Ninja probe said `myBoard.turn != team: False`: the player was Ivory and owned
the first Ban. The visible Ban action was also present; the detector had missed
it. After the local clock expired, the app auto-banned Giant and deducted 6
rating. Phase ownership now comes directly from that native turn predicate.
For actual opponent bans, the public `TEXURE ASSIGNED TO <piece>` event following
`OnBanCharacter` supplies the exact character; whole-pot differencing is only a
compatibility fallback. No engine moves were played, so this is another
controller failure rather than a strength result.

The immediate retry found a locally usable Ban turn, submitted Ninja, and
received its acknowledgement. Unity then printed the same
`NetworkManager:OnBanCharacter(Type)` method frame again beneath the texture
assignment from that one callback. The phase loop mistook the duplicate stack
frame for the opponent's following Ban and stopped when no second named texture
event existed. Callback frames are now debounced so one network Ban can advance
exactly one phase.

The user then manually completed this same match while passive raw and timed
Unity captures ran. That complete trace supplied two further corrections. The
remote Angel Ban had already completed during pot calibration; the later usable
Ninja touch was local phase one, so labeling the player Ivory from current-turn
state alone was wrong. Side detection now binds the native turn predicate to a
non-consuming unique-Ban count, while a parallel named-Ban journal replays
Angel into engine phase zero even though its queue event preceded calibration.
Second, each `Square.Spawn` sequence is a new immutable group delta, not a
cumulative redraw. The captured public opponent
groups were 35 points (Pawn, Knight, Bishop, Queen), then +40 (Checker, two
Turtles, Berserker, Parasite), then +25 (Pawn, Bishop, Rook). Automation now
accumulates those deltas; the opening fixed King alone defines that trace's King
hypothesis. The local middle window independently accepted +44 from 27 to 71,
matching phase seven's 40-point minimum and 90-point cumulative cap.

The manually played game ended in **Victory, enemy forfeited, +13 ranking**.
The production result OCR classified the captured overlay as `win`. This is a
protocol/result fixture, not an Ultimate Fish strength result, because the user
drafted and played after the controller stopped.

## Ranked acknowledgement correction — 2026-08-05

The next autonomous attempt accepted a match, recovered the opponent's Devil
ban, classified the local side as Onyx, and submitted Ninja for phase one. Its
opening public group was exactly **Queen 17 + Jester 10 + fixed King**, matching
the cumulative 27-point log and proving that an earlier pot-overlap tap result
of Jester + Ghost was false. The group had no timely `OnSanityCheck`; that event
arrived only after the following local placement. The public spawn journal now
completes after 120 ms without another spawn record, and Ranked has no tap
fallback.

Ultimate Fish selected Prince 18 + Sniper 17 + CopyCat 5. The native app
acknowledged Prince and Sniper for 35 cumulative points. CopyCat's wide drag
landed one file inward onto Prince's mirror conflict and was despawned, but the
old controller neither verified the landing nor recognized the icon-only green
Lock control. The app eventually auto-locked the valid 35-point group. The new
path records each native landing coordinate, reserves that actual footprint,
requires every material increment, retries a rejected drag on a different cell,
checks the exact cumulative total, and detects the green checkmark geometrically.

The user continued this diagnostic match to preserve the full trace. It ended
in **Victory, opponent timeout, +19 ranking, two-win streak**. The real overlay
also exposed an OCR failure: unprocessed Tesseract returned no result for the
extruded white headline. Production classification now retries isolated bright
lettering and returns `win` on the captured frame. As with the preceding trace,
this is protocol/result evidence rather than an engine-strength win because the
controller stopped during drafting and the user completed play.

A subsequent fully autonomous retry joined matchmaking without rebuilding an
army, accepted the offer, recovered the opponent's Devil ban, classified Onyx,
and submitted Ninja. The opponent's opening group announced 8 then 18 material.
`OnSanityCheck` followed after 82 ms, but its public Mage and royal spawn records
did not begin for another 357 ms. The controller stopped before making its local
pick because it had treated the early sanity event as a completed empty journal.
The captured regression now keeps polling until the candidate reconciles
exactly as Mage 8 + Jester 10 (plus the fixed King). No engine move was played,
so this remains a controller/protocol failure rather than a strength result.
The user manually finished the orphaned match, which ended in a checkmate loss;
that result is excluded from engine-strength scoring for the same reason.

The corrected retry accepted another match immediately. It recovered an Onyx
draft with Devil/Ninja opening bans, waited through the early Sanity marker, and
reconciled the opponent's 36-point opening as Jester + two Rooks. Ultimate Fish
then drafted Prince 18 + Sniper 17 + CopyCat 5, measured Prince landing at e3
instead of requested d3 and CopyCat at g2 instead of h2, verified all 40 points,
and locked the group successfully. This is the first autonomous live Ranked
pick to receive every native placement and Lock acknowledgement.

The opponent next banned Sniper and Ultimate Fish selected Prince for its ban.
The app displayed the valid fixed blue Ban control at `(622,208)`, but the old
pot-relative red detector repeatedly selected a lock-chain component at
`(909,743)` until the clock expired. The captured native decompilation confirms
that the pot remained usable; this was UI targeting, not a ban-legality rule.
The controller now prioritizes the fixed blue component, reproduces its exact
captured coordinate, and retains native acknowledgement as the phase boundary.
The user manually completed the orphaned match and lost. No engine move was
played, so this attempt is still excluded from strength data.

## Native information-set search checkpoint — 2026-08-04

The former Python plurality/top-three audit and repetition replacement were
removed. The C++ engine now owns the entire common-root decision across the
retained public-information set. It shallow-searches every common action in
every belief, then deep-searches the robust shortlist across eight evenly
spaced representatives while forcing each candidate's worst shallow belief
into its deep sample. An adversarial regression proves that a concrete safe
world takes an exposed Pawn with a continuing attack while the two-world search
rejects that root when another legal world contains the hidden-Ghost recapture.
A second regression
places possible Ghosts adjacent to a winning Queen move and proves that the
engine still captures the real King; no hardcoded material/adjacency filter is
present.

On a 64-belief, 45-common-root stress position using the live Giant roster:

| Limit | Wall time | Aggregate nodes | Result |
| --- | ---: | ---: | --- |
| depth 6 | 4.82 s | 7,767,979 | `b3-c4`, worst +1.28 |
| depth 7 | 10.30 s | 17,107,021 | `b3-c4`, worst +1.28 |

The new build then won three fresh Very Hard CPU games at depth 6. The set
exercised a 64→56→2 hidden-Ghost expansion/collapse sequence, exact Ghost
attacks, calculated recapturing sacrifices, and forced-win reporting. Two
Unranked opponents subsequently left before making a move and are recorded only
as controller/queue successes, not strength evidence. A separate Onyx game was
lost on time after own-army verification rejected neighboring collider spill;
that controller failure is excluded. The verifier now requires two expected
native identities without treating an incidental adjacent selection as a
roster mismatch, and a fresh Onyx board passed all 13 own-cell checks before
making a legal engine move.

## Adversarial setup and clock checkpoint — 2026-08-05

The follow-up coevolution league added a live Unranked adversary with an
off-corner King, five Snipers, and a Berserker. The winning Dragon/Giant setup
finished the 8-generation final at **17W-10D-1L**. In a separate 20,000-node
common pool it scored **11W-3D-0L**, compared with **9W-4D-1L** for the prior
five-Queen/Checker live setup. The reproducible population, hall of fame, and
finalist records are in `evolved-army-adversarial-2026-08-05.json`.

Native belief search now charges its complete-set shallow audit against the
requested move time. The forcing-reply audit uses depth one plus quiescence,
which still resolves hidden-Ghost recaptures in every retained world. On the
measured 64-belief, 48-common-root Unranked opening, a 7-second request changed
from **11.403 s / depth 3 / 1,698,263 nodes** to
**7.021 s / depth 4 / 1,050,094 nodes**, with the same `g1-f3` root. Public
threefold-completing roots are also scored as draws by native search instead
of being replaced by controller policy.

The resulting build won three fresh Very Hard CPU games by checkmate at a
10-second ceiling. Those games exercised 64-world Ghost sets, King/Jester
ambiguity, public Ghost reveals and captures, Mage swaps, Copycat clones,
Prince continuations, and Giant collisions. A subsequent played Unranked game
recovered from an opening Sniper shot and repeated Rook exchanges, reached a
native forced mate in five, and ended when the opponent resigned. Opponent
opening timeouts remain classified separately from strength wins.

## Native castling correction — 2026-08-05

The Ranked follow-up established that banning an already-drafted type is
legal; the failed Prince Ban was solely a controller target-selection error.
The user manually completed that orphaned match and lost, with no engine move
played, so it remains excluded from strength scoring.

A native-code audit then found a genuine missing rules branch. Both King and
Jester are constructed as `SimulatedKing` and may castle toward the first
unmoved Rook on either horizontal ray when it is at least three files away.
The royal moves two files and the Rook moves beside it. The shipping generator
does not require a friendly Rook and tests neither the starting nor crossed
square for attack; only ordinary post-move real-King safety applies. Ultimate
Fish now reproduces that behavior, moves both pieces atomically, retains moved
state in UPN/undo/hash state, and uses ordinary `d1-f1` notation.

The Android controller now waits through the automatic Rook animation before
starting the opponent loop. Its Onyx opening rewind either consumes the exact
companion Rook callback or retains every legal public origin if that callback
is absent. No castling action was present in the 53 locally saved replays, so
the recovered simulation and live handler are the conformance authority for
this branch. The post-correction deterministic benchmark is classic
perft(3) **8,900** and Ultimate perft(1/2/3) **44 / 1,936 / 74,084**; the full
native and 108-test phone suites pass.

The immediate post-correction Ranked retry recovered an Onyx draft and the
opponent's Ninja Ban, then selected Prince as the local Ban. The top blue Ban
label was still visible for Ninja because side detection had touched that pot.
`_ban_ranked_piece` clicked the stale label before selecting Prince and spent
its clock retrying the same no-op. A manual Prince/confirm tap after the
controller exited advanced the draft, but the unattended following Pick timed
out for a **-5 ranking** defeat. No engine move was played. Local Bans now
always touch the requested pot first and wait for Unity's native local-turn
predicate before attempting confirmation; a regression test preserves that
pot-then-control ordering.

A second live retry proved that the earlier fixed-blue-control diagnosis was
also wrong: the top blue `BAN` element is only a phase label. Selecting Ninja
correctly produced the native local-turn predicate, but tapping that label did
not submit the Ban. The user preserved the diagnostic draft by pressing the
actual large red **Ban** button in the lower-left piece inspector. The
controller now detects that red control by its fixed lower-left geometry and
rejects both the blue phase label and the selected-pot speech bubble. It has no
speech-bubble or OCR fallback: a late inspector frame is retried without
injecting a decoy tap. The full 110-test phone suite covers the captured
geometry, explicit decoy rejection, and the exact
pot-selection-then-red-control order.

The first fully autonomous retry with that correction was Ivory and submitted
Ninja successfully, observed the opponent's Prince Ban, locked its complete
40-point opening group, observed the opponent's 36-point opening group, and
submitted Sniper in the second Ban window. The middle placement then exposed a
separate controller defect. Native point logs proved both Copycats cost exactly
the recovered **5** points (`32 -> 37` and `72 -> 77`); no dynamic-cost rule is
involved. A Pawn drop produced no point increase, and retries against cells
covered by pending models first removed 17 points and then accumulated several
3-point Pawns. The final 78-vs-80 guard correctly refused to Lock, but only
after the pending board was already corrupted. Ranked placement now orders
small models first, maximizes target-cell clearance, and requires the exact
per-character point delta. A nonzero wrong delta fails immediately instead of
being retried on a mutated board. This diagnostic run contains no engine move
and remains excluded from strength scoring.

The next autonomous run validated the repair across all twelve phases as Onyx.
It submitted all three Bans, locked a 40-point opening group, a 50-point middle
group under the Onyx 90 cap, and its final 10 points. Harmless intercepted
drops produced zero material and were retried; every accepted piece matched
its exact native cost. The completed 100-point deployment included two
Copycats, two Jesters, Prince, Sniper, Dragon, Queen, and Pawn. The controller
then waited 30 seconds for `Board.LoadBoard`, while the opponent's opening move
began and the app never emitted that callback. The live trace proves Ranked
reuses `LoadBoardDraft`: final `OnSpawnPieceGroup` is the reveal barrier, with
gameplay starting after the intro on the same board. The nonexistent load wait
is removed, Ranked calibration now spans that reveal interval, and its
non-consuming journal retains any opening move. This run also contains no
engine move and is excluded from strength scoring; the abandoned live match
eventually flagged for **-5 ranking**.

The immediate reveal-fixed retry completed the first fully autonomous Ranked
game from queue through result. As Onyx, Ultimate Fish drafted and placed a
100-point roster containing Prince, Sniper, Dragon, Ghost, three Copycats and
two Jesters. It recovered the opponent's opening from the public journal,
initialized one fully determined belief, and searched every action at a
10-second iterative-deepening ceiling. Search reached depths 5–8 at roughly
125k–212k nodes per request, handled a Prince continuation as two native
actions, and grew its evaluation from +0.29 to +18.45. The opponent resigned
after `d4-c5, c5-b6`; the public overlay recorded **Victory — enemy forfeited,
+13 ranking**. This is the first Ranked result included in engine strength
scoring: **1 win, 0 draws, 0 losses**.

The following repeatability run completed all twelve phases as Ivory, but the
opponent legally stopped after adding an 8-point Mage for **88 total points**.
The controller had retained that public total yet `initialize_ranked_public`
passed the global 100-point cap into belief construction, fabricating twelve
unexplained hidden points and failing before the first engine move. Ranked
belief initialization now uses the final observed opponent total. A regression
fixture preserves an 88-point final army; the full phone suite passes 114
tests. The abandoned live game later flagged for **-10 ranking** without an
engine move and is excluded from engine strength scoring.

The next Ivory draft live-validated both corrections. CopyCat changed material
by its static **5** points while a Penguin drop emitted no point change; the
controller retried that zero-delta failure and placed the Penguin on a clear
cell, completing an exact 100-point roster. Gameplay initialized from the
public final-spawn journal and the engine recovered from -2.32 to +4.51. A
later `e6-f6` CopyCat action exposed a Unity collider hazard: the tap aimed at
e6 reported logical pointer square h2 and committed the different linked pair's
`h2-h3` move. Selection now consumes the preceding native
`Square.OnPointerDown` coordinate, requires it to equal the engine source, and
tries bounded offsets inside that cell on interception. Piece-type equality is
not enough when several CopyCats are present. The 116-test phone suite includes
the captured e6/h2 regression. Because the controller stopped after the
unintended move, this game is excluded from engine strength scoring.

A clean Onyx retry then validated exact selection across several linked-half
actions and reached +8.03, but stopped responding after the opponent's
Parasite attacked a CopyCat clone. The native trace explains the apparent
strength inversion: Parasite `d5-d4` was publicly reported by the online
release diagnostic, killed itself, and recolored the clone in place, so Unity
emitted no ordinary `parasite moves X --> Y` record. `ChangeTurn End` arrived,
but the controller treated it as a continuation and waited through the local
clock. Material changed **74/95 -> 64/90**, exactly the opponent losing its
15-point Parasite while gaining the full 5-point CopyCat pair. The controller
now applies an unlogged visible Parasite action from the public release record
at that turn barrier. The engine also transfers both linked CopyCat halves
atomically when either half is possessed; its prior single-half recolor did not
match the native material transition. Native conformance tests and the
117-test phone suite pass. The timeout cost **-5 ranking** and is excluded from
engine strength scoring because the corrected engine never received its turn.

An evolved-roster Very Hard CPU validation next exposed a CopyCat controller
assumption. Selecting a `d3/e3` pair and moving `d3-e4` emitted no companion
movement callback; its following legal-dot sources were `e4/e3`. The partner
was unavailable, so the native conditional-pair rule left it stationary and
the two halves ceased to be file mirrors. The controller now resolves the
partner's current square through the lossless UPN link rather than reflecting
the selected source file.

A later corrected-engine CPU retry supplied the complementary native case.
The CPU selected its `d8/e8` pair and moved the clone `e8-f8`; Unity immediately
emitted the companion primary move `d8-c8` before `ChangeTurn End`. That cleared
the diagonal for a later Bishop capture `f10-a5`. This proves the complete
rule: an available partner mirrors the displacement from its current square,
while a frozen/cooldown partner stays put. Two intermediate commits preserve
the rejected independent-half interpretation as audit history; their generated
evolution artifacts are deliberately excluded from the repository and policy
promotion.

The restored conditional-pair engine passed the native suite and all 185 phone
and evolution tests. A fresh 12-generation, 16-policy league completed at
5,000 nodes per game, followed by six 30,000-node finalists. Generation nine
was strongest at **18 wins, 11 draws, 1 loss**; the deeper finalists drew all
ten games apiece and converged on the same 100-point deployment:
`King a1; Queens c1/f3/c3/d2; CopyCats b1/d3/d1; Giants a2/g2; Penguin e2`.
The complete reproducible run is stored in
`evolved-draft-conditional-copycat-2026-08-07.json`.

That exact roster then won **3/3** fresh Very Hard CPU games with a 10-second
iterative-deepening ceiling. The games exercised both CopyCat branches: a
frozen partner remained stationary and an available enemy pair completed both
half-moves. They also covered hidden Ghost revelation, Mage play, and forced
mate/checkmate endings without a controller or position mismatch.

A separate live Unranked validation also finished **3 wins, 0 draws, 0
losses**, spanning Ivory and Onyx, one- and two-state public royal hypotheses,
and opposing Dragon/Queen/Checker and Bishop/Queen/Giant families. The native
draft policy and Python continuation baseline now use the converged finalist's
pick, repeat, global-ban, opponent-denial, self-preservation, and final-Penguin
weights. Its deterministic self-draft fills both teams to 100 points and bans
Prince, Mage, Bomb, Jester, Ghost, and Sniper in that opponent-aware sequence.

The first subsequent Unranked validation found an initialization-only edge
case before Ultimate Fish received a turn. As Onyx, the controller scans the
opponent's three deployment ranks after Ivory has moved. Both halves of an
opening CopyCat action can therefore finish on rank 7 and disappear below the
probe area. The two public native movement callbacks nevertheless provide the
exact type and source for each half, so rewind now restores those sources
directly when no scanned destination exists. A regression fixture covers the
captured `h8-g7` / `a8-b7` pair; no hidden-state inference is involved.

The first post-promotion Ranked batch is **not** strength evidence. Its true
record was **0 wins, 1 stalemate, 1 loss**; both games were losing. The result
reader had consumed `GameOver MESSAGE` before Unity's later `STALEMATE` label,
failed to OCR the still-animating overlay, and incorrectly applied a generic
win fallback. `STALEMATE` is now an explicit draw token, unknown OCR remains
unknown, and a checkmate/knockout received on the opponent-action side resolves
as a loss. The loss and stalemate also repeated a draft/deployment weakness:
the evolved greedy policy locked geometry that could not fit its two final
CopyCats, adapted to a 95-point Bishop, and began around -13.5.

Ranked choice is therefore no longer one-ply policy selection. A bounded
adversarial macro search generates whole legal Ban/Pick actions, minimizes over
the opponent's next public action, rolls each branch through all twelve native
windows, and rejects any terminal roster that cannot fit around the exact
already-locked local geometry. Surviving starting positions are evaluated by
Ultimate Fish at 10,000 nodes; the evolved weights only order the five-by-three
beam. The phase-zero reference search evaluates 15 leaves in **4.5 seconds**.
In the captured Onyx draft after `Ghost/Mage` bans and the opponent's
`Jester+Queen` opening, the wider 20,000-node reference chooses `Prince,
Prince, Checker, Giant, Giant` with a -0.40 worst-leaf evaluation and jointly
reserves the six later wide/ordinary footprints. This replaces the repeated
95-point line whose live evaluation was below -13.

The first live searched draft then faced a different 40-point
`Giant/Knight/Parasite/Prince` opening. Ultimate Fish banned Mage and chose the
same two-Prince/two-Giant family. A rollout bug initially checked only the
policy's first completed roster; if that roster did not fit the native Giant
landing, later searches fell back instead of backtracking to another legal
completion. Even with those fallbacks, the resulting 99-point army began at
**+0.41**, rose to **+4.04**, and played `h1-h10` at **mate 1**. The retained
public result screen proves **Knock-Out** after that local move: a win. Terminal
rollout filtering now backtracks across alternate completions, and the reduced
beam finds a feasible middle action in **13.7 seconds** instead of exhausting
the phase. Result classification also waits ten seconds for the condition
menu, which this game did not finish opening until 4.3 seconds after the early
network event.

The next searched Ranked attempt is also recorded as a **loss**, not engine
strength evidence. During Onyx's 45-point middle group, the first CopyCat
landed successfully but its optional move-to-planned-square correction selected
an overlapping locked Giant collider. The controller treated that unchanged-
material interception as fatal. Unity auto-locked the five valid additions at
85 total points; with the controller gone, the following Ban and final Pick
windows expired and the app reported **Defeat — You've run out of time**. The
public final enemy group was recovered exactly from native spawn records as
`Checker, Pawn, Pawn, Rook` (100 total). A successful native landing is now
retained when its correction ray is intercepted by a different locked model,
and the planner recomputes the remainder of the group around that exact square.
The regression brings the complete suite to 194 passing tests.
